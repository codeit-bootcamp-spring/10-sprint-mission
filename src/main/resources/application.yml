spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/discodeit # DB?? ?? ??
    username: discodeit_user
    password: discodeit1234
    driver-class-name: org.postgresql.Driver
  data:
    web:
      pageable:
        one-indexed-parameters: true #page = 1로 요청 들어오면 실제 pageNumber = 0으로 처리
  #    url: jdbc:h2:mem:test
  jpa:
    hibernate:
      ddl-auto: none # ?? ??? update, ??? none ??
    show-sql: true       # ?? ?? ??

  sql:
    init:
      mode: always

discodeit:
  storage:
    type: local
    local:
      root-path: storage

server:
  port: ${PORT:8080}


