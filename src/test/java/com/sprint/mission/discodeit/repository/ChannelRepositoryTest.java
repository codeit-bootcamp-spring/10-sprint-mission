package com.sprint.mission.discodeit.repository;import com.sprint.mission.discodeit.config.AppConfig;import com.sprint.mission.discodeit.entity.ChannelEntity;import com.sprint.mission.discodeit.entity.ChannelType;import com.sprint.mission.discodeit.entity.ReadStatusEntity;import com.sprint.mission.discodeit.entity.UserEntity;import org.junit.jupiter.api.DisplayName;import org.junit.jupiter.api.Test;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.boot.test.autoconfigure.jdbc.AutoConfigureTestDatabase;import org.springframework.boot.test.autoconfigure.orm.jpa.DataJpaTest;import org.springframework.context.annotation.Import;import org.springframework.test.context.ActiveProfiles;import java.time.Instant;import java.util.List;import static org.junit.jupiter.api.Assertions.*;@DataJpaTest@ActiveProfiles("test")@Import(AppConfig.class)@AutoConfigureTestDatabase(replace = AutoConfigureTestDatabase.Replace.NONE)public class ChannelRepositoryTest {    @Autowired    private ChannelRepository channelRepository;    @Autowired    private UserRepository userRepository;    @Autowired    private ReadStatusRepository readStatusRepository;    /*        다건 조회        -------        해당 사용자가 참여하고 있는 공개 채널 + 비공개 채널     */    // [성공]    @Test    @DisplayName("사용자 전체 채널 목록 조회 완료")    void find_all_visible_channel_by_user_id_success() {        // given        // 사용자        UserEntity firstUser = new UserEntity(                "yushi",                "yushi@wish.com",                "yushi1234"        );        userRepository.save(firstUser);        UserEntity secondUser = new UserEntity(                "tokuno",                "tokuno@wish.com",                "tokuno1234"        );        userRepository.save(secondUser);        // 채널        ChannelEntity firstChannel = new ChannelEntity(                "Alien",                "To be Alien together",                ChannelType.PRIVATE        );        ChannelEntity secondChannel = new ChannelEntity(                "Meow World",                "cat is my god",                ChannelType.PUBLIC        );        ChannelEntity thirdChannel = new ChannelEntity(                "DueDue",                "ByaByaBya",                ChannelType.PRIVATE        );        List<ChannelEntity> channels = List.of(firstChannel, secondChannel, thirdChannel);        channelRepository.saveAll(channels);        // 읽음 상태        List<ReadStatusEntity> readStatus = List.of(                new ReadStatusEntity(firstUser, firstChannel, Instant.now()),                new ReadStatusEntity(firstUser, secondChannel, Instant.now()),                new ReadStatusEntity(secondUser, thirdChannel, Instant.now())        );        readStatusRepository.saveAll(readStatus);        // when        List<ChannelEntity> result = channelRepository.findAllVisibleChannelByUserId(firstUser.getId());        // then        assertEquals(2, result.size());        assertTrue(result.stream().anyMatch(c -> c.getName().equals("Alien")));        assertTrue(result.stream().anyMatch(c -> c.getName().equals("Meow World")));        assertFalse(result.stream().anyMatch(c -> c.getName().equals("DueDue")));    }    // [실패]    @Test    @DisplayName("사용자가 참여하고 있는 채널이 없을 경우, 빈 리스트 반환")    void find_all_visible_channel_by_user_id_failure() {        // given        // 사용자        UserEntity user = new UserEntity(                "yushi",                "yushi@wish.com",                "yushi1234"        );        userRepository.save(user);        // when        List<ChannelEntity> result = channelRepository.findAllVisibleChannelByUserId(user.getId());        // then        assertTrue(result.isEmpty());    }}