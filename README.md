# 🚀 Discodeit Project - Sprint 3

스프린트 3에서는 스프링 프레임워크를 도입하여 **IoC/DI를 통한 객체 관리**를 자동화하고, **비즈니스 로직 고도화** 및 **다중 저장소(JCF/File) 선택 기능**을 구현하는 데 집중했습니다.

---

## ✅ 완료된 요구사항 (Milestones)

### 1. Spring 프레임워크 도입 및 설정
- [x] Spring Boot 3.4.0 기반 프로젝트 초기화 및 YAML 설정 전환.
- [x] Lombok(@Getter, @RequiredArgsConstructor)을 통한 코드 최적화.
- [x] IoC Container 및 DI를 활용한 Service/Repository Bean 등록.

### 2. 도메인 및 비즈니스 로직 고도화
- [x] **시간 타입 표준화**: 가독성 및 확장성을 위해 모든 시간 필드를 `Instant`로 통일.
- [x] **신규 도메인 확장**: `ReadStatus`, `UserStatus`, `BinaryContent` 모델 추가.
- [x] **데이터 연동**: 
    - 온라인 상태 판단 로직 구현 (마지막 접속 5분 이내).
    - 유저/채널 삭제 시 연관 데이터(메시지, 첨부파일 등) 연쇄 삭제 처리.
- [x] **DTO 패턴 적용**: 서비스 레이어 파라미터를 DTO로 그룹화하여 캡슐화 강화.

### 3. 유연한 인프라 관리 (심화)
- [x] **동적 Bean 등록**: `application.yaml`의 `discodeit.repository.type` 값에 따라 JCF와 File 저장소 타입을 실행 시점에 결정.
- [x] **외부 설정 주입**: `@Value`를 활용하여 소스 코드 수정 없이 파일 저장 경로 제어.

---

## 🏗 시스템 아키텍처 및 핵심 개념
- **IoC Container**: 개발자가 직접 객체를 생성하지 않고 스프링 컨테이너가 Bean의 생명주기를 관리합니다.
- **Dependency Injection**: 생성자 주입을 통해 서비스가 필요한 레포지토리 의존성을 자동으로 할당받습니다.
- **Strategy Pattern**: 동일한 인터페이스를 구현한 JCF와 File 레포지토리를 설정에 따라 교체하여 유연성을 확보했습니다.



---

## 📂 프로젝트 구조
```text
src/main/java/com/sprint/mission/discodeit/
├── service/         # 비즈니스 로직 및 예외 처리
├── repository/      # JCF, File 기반 저장소 구현체
├── dto/             # 데이터 전송 객체 (Request/Response)
├── entity/          # 도메인 엔티티 및 핵심 모델
└── DiscodeitApplication.java # 스프링 부트 실행 및 테스트
